#include <stdio.h>
#include <sys/ipc.h>
#include <sys/shm.h>
int main()
{
    int id;
    char *data;
   id = shmget(1234, 100, 0666 | IPC_CREAT);
   data = shmat(id, NULL, 0);
   sprintf(data, "Hello Shared Memory");
   printf("Data = %s\n", data);
    shmdt(data);
    return 0;
}
